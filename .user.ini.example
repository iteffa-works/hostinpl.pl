; Copy to project root as .user.ini for OSPanel / local PHP 8.1+
; Suppresses legacy deprecations without changing panel code.
display_errors = Off
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
